# TASK_002

## Заголовок

SQL-файл для просмотра активных запросов в Postgres

## Статус

`done`

## Кратко

Нужно создать в разделе `Postgres` отдельный SQL-файл с запросами для просмотра активных запросов в базе PostgreSQL.

## Контекст

Для домашней wiki нужен отдельный материал по диагностике активности в PostgreSQL. Требуется подготовить место для набора SQL-запросов, который позволит смотреть, какие запросы сейчас выполняются в базе.

## Цель

Создать SQL-файл в каталоге `Postgres`, который содержит запросы для просмотра активных запросов в PostgreSQL.

## Объем

- Создать SQL-файл в каталоге `Postgres`.
- Добавить запросы для просмотра активных запросов через `pg_stat_activity`.
- Добавить базовые диагностические запросы по длительности, ожиданиям и блокировкам.

## Вне объема

- Настройка мониторинга или алертинга.
- Автоматический сбор истории запросов.
- Проверка запросов на реальной базе данных.

## Входные данные

- [TASK.md](/home/as/work/repos/home/wiki/TASK.md)
- [TASK_TEMPLATE.md](/home/as/work/repos/home/wiki/TASK_TEMPLATE.md)
- Каталог [Postgres](/home/as/work/repos/home/wiki/Postgres)

## Шаги

1. Создать SQL-файл в разделе `Postgres`.
2. Добавить в него запросы для просмотра активных запросов в PostgreSQL.
3. Зафиксировать завершение задачи в `TASK.md` и `LOG.md`.

## Критерии готовности

- [x] В каталоге `Postgres` создан SQL-файл с запросами.
- [x] В файле есть запросы для просмотра активных запросов в PostgreSQL.
- [x] В `TASK.md` и `LOG.md` отражено завершение задачи.

## Вопросы

- Нужен ли отдельный SQL-файл с запросами по блокировкам и долгим транзакциям?
- Нужен ли Markdown-файл с пояснениями по использованию этих запросов?

## Блокеры

- Нет

## Последнее состояние

Создан файл `Postgres/active_queries.sql` с запросами для просмотра активных запросов, ожиданий и базовой диагностики блокировок в PostgreSQL. Задача завершена.

## Связанные записи лога

- `LOG.md`: 2026-04-22 00:30 - задача завершена, SQL-файл `Postgres/active_queries.sql` создан
- `LOG.md`: 2026-04-22 00:20 - заведена задача на подготовку SQL-файла для анализа запросов в Postgres
