# LOG

Журнал прогресса по задачам. Нужен для передачи контекста между сессиями и фиксации точки остановки.

## Правила

- Новые записи добавляются сверху.
- Каждая запись ссылается на ID задачи.
- Запись должна отвечать на три вопроса: что сделано, где остановились, что дальше.
- Если есть блокер, он фиксируется явно.

## Шаблон записи

### YYYY-MM-DD HH:MM - TASK_XXX - status

- Что сделано: ...
- Где остановились: ...
- Что дальше: ...
- Блокеры: нет

## Записи

### 2026-08-17 17:41 - TASK_016 - done

- Что сделано: создан `Redis/backup-restore-kubernetes.md` с полной инструкцией резервного копирования и восстановления Redis в Kubernetes; `Redis/README.md` преобразован в индекс раздела со ссылкой на новый материал; `TASK_016.md` закрыт и перенесен в `Tasks/`.
- Где остановились: документация раздела Redis приведена к общей структуре wiki; процедура восстановления Redis содержательно не менялась.
- Что дальше: при необходимости добавить отдельные Redis-материалы по мониторингу, persistence, Sentinel или Cluster.
- Блокеры: нет.

### 2026-08-17 17:41 - TASK_016 - in_progress

- Что сделано: заведена задача `TASK_016.md` на перенос полной инструкции из `Redis/README.md` в отдельный материал раздела и приведение README к индексному формату.
- Где остановились: постановка задачи оформлена; перенос инструкции и обновление ссылок еще не завершены.
- Что дальше: создать `Redis/backup-restore-kubernetes.md`, заменить `Redis/README.md` индексом, обновить `TASK.md` и закрыть задачу.
- Блокеры: нет.

### 2026-08-17 16:36 - TASK_015 - done

- Что сделано: создан `Kubernetes/cilium-vip-and-argocd-hostname.md` с runbook смены VIP `192.168.215.9 -> 192.168.215.18`, hostname Argo CD, DNS/Keycloak, диагностики `argocd-repo-server` в `Init` и фикса `download-tools` без `dl.k8s.io`; в исторический runbook Cilium 1.20 добавлена ссылка на актуальные VIP и hostname; обновлен индекс Kubernetes; `TASK_015.md` закрыт и перенесен в `Tasks/`.
- Где остановились: документация готова; живой кластер и инфраструктурные репозитории в рамках этой wiki-задачи не изменялись.
- Что дальше: отдельно проверить DNS `argocd-dev.oro.moscow -> 192.168.215.18` и redirect URI клиента Keycloak `argocd-dev`.
- Блокеры: нет.

### 2026-08-17 16:29 - TASK_015 - in_progress

- Что сделано: создан `TASK_015.md` на подготовку безопасного runbook выполненного cutover VIP Gateway `192.168.215.9 -> 192.168.215.18`, hostname Argo CD `argocd-talos-dev.oro.moscow -> argocd-dev.oro.moscow` и исправления зависания `argocd-repo-server` в `Init` на `dl.k8s.io`.
- Где остановились: постановка задачи оформлена; основная инструкция в разделе `Kubernetes` еще не создана.
- Что дальше: подготовить `Kubernetes/cilium-vip-and-argocd-hostname.md`, обновить индекс раздела, проверить Markdown и закрыть задачу.
- Блокеры: нет.

### 2026-08-14 16:12 - TASK_014 - done

- Что сделано: создан `Kubernetes/talos-cilium-1.20-grpc-web-migration.md` с полным безопасным runbook миграции Kubernetes, Cilium, Gateway API и Argo CD; добавлены команды preflight, backup, временного доступа к Talos API, трех minor upgrades, переключения с rollback, проверок gRPC-web и cleanup; обновлен индекс Kubernetes.
- Где остановились: документация готова, основной Markdown прошел markdownlint, `TASK_014.md` закрыт и подготовлен к переносу в `Tasks/`; живой кластер и инфраструктурные репозитории в рамках этой wiki-задачи не изменялись.
- Что дальше: восстановить прямой сетевой доступ к Talos API `:50000` отдельной задачей и перенести etcd snapshot из `/tmp` в одобренное зашифрованное хранилище с установленным retention.
- Блокеры: нет.

### 2026-08-14 16:08 - TASK_014 - in_progress

- Что сделано: создан `TASK_014.md` на подготовку безопасного runbook выполненной миграции Kubernetes `1.32.3 -> 1.35.7`, Cilium `1.19.6 -> 1.20.0` и исправления gRPC-web для Argo CD; зафиксированы требования к backup, rollback, временному доступу к Talos API, проверкам и очистке.
- Где остановились: постановка задачи оформлена; основная инструкция в разделе `Kubernetes` еще не создана.
- Что дальше: подготовить `Kubernetes/talos-cilium-1.20-grpc-web-migration.md`, обновить индекс раздела, проверить Markdown и закрыть задачу.
- Блокеры: нет.

### 2026-07-20 16:26 - TASK_013 - done

- Что сделано: по решению владельца ротация credential и rollout ESO/Vault auth в живых окружениях исключены из объема; документационная часть принята завершенной, `TASK_013.md` переведен в `done` и перенесен в `Tasks/`.
- Где остановились: инструкция, обезличенные примеры и проверки пилота `points-adm` готовы; задача закрыта без изменений живой инфраструктуры.
- Что дальше: использовать `Vault/kubernetes-external-secrets.md` как runbook, если внедрение в `stage`, `prod` или `cloud` будет согласовано отдельной задачей.
- Блокеры: нет.

### 2026-07-20 16:22 - TASK_013 - blocked

- Что сделано: создана инструкция `Vault/kubernetes-external-secrets.md` и обезличенный Helm chart в `Vault/examples`; обновлены README; chart прошел `helm lint`, `helm template` и server-side dry-run. В живом `dev` подтверждены Vault `1.20.1`, ESO `2.1.0`, валидный `ClusterSecretStore`, `SecretSynced`, `envFrom`, наличие семи переменных без вывода значений, успешный rollout и статус Argo CD `Synced/Healthy`. `Vault/pre_vault.md` отсутствует в истории Git и добавлен в `.gitignore`.
- Где остановились: документация и пилот `points-adm` в `dev` готовы; ESO отсутствует в `stage`, `prod`, `cloud`; раскрытые учетные данные не ротированы из-за отсутствия административных доступов к Vault и исходным системам.
- Что дальше: ротировать credential в PostgreSQL, Kafka, Keycloak и DWH, затем через GitOps установить ESO и настроить отдельные Vault auth mount/policy/role в `stage`, `prod`, `cloud`; после подтверждения перенести `TASK_013.md` в `Tasks/`.
- Блокеры: нет Vault credentials, доступов к исходным системам и GitOps-репозитория Argo CD; rollout в живые окружения требует согласования.

### 2026-07-20 16:02 - TASK_013 - todo

- Что сделано: изучен `Vault/pre_vault.md`; заведена задача `TASK_013.md` на подготовку инструкции по интеграции единого Vault в `cloud` с четырьмя Kubernetes-кластерами через External Secrets Operator, Helm и Argo CD.
- Где остановились: зафиксированы фактическое состояние пилота `points-adm`, работающая синхронизация Secret через ESO, отсутствие `envFrom` в Deployment, ошибка структуры values и требования к ротации раскрытых учетных данных; основная инструкция еще не создана.
- Что дальше: проверить версии и текущую конфигурацию Vault/ESO, ротировать раскрытые секреты, исправить Helm-шаблон и подготовить материал `Vault/kubernetes-external-secrets.md` с обезличенными примерами.
- Блокеры: для проверенных манифестов и end-to-end проверки нужны фактические версии компонентов и доступ к Vault, Argo CD и Kubernetes-кластерам.

### 2026-07-16 10:47 - TASK_012 - done

- Что сделано: созданы материалы `Ansible/install-venv.md`, `Ansible/run-playbook.md`, `Ansible/ssh-troubleshooting.md`; добавлены примеры `Ansible/ansible.cfg.example`, `Ansible/hosts.ini.example`, `Ansible/site.yml.example`; обновлен `Ansible/README.md`.
- Где остановились: задача завершена, материалы по базовым командам Ansible добавлены в wiki.
- Что дальше: при необходимости добавить отдельные материалы по структуре Ansible-проекта, ролям, group_vars, Ansible Vault и collections.
- Блокеры: нет

### 2026-07-16 10:43 - TASK_012 - todo

- Что сделано: заведена задача `TASK_012.md` на подготовку отдельных материалов по Ansible: установка в Python venv, запуск playbook и диагностика SSH-ошибок `Host key verification failed` / `ssh_askpass`.
- Где остановились: постановка задачи готова, материалы в разделе `Ansible` еще не созданы.
- Что дальше: создать файлы `Ansible/install-venv.md`, `Ansible/run-playbook.md`, `Ansible/ssh-troubleshooting.md` и примеры `ansible.cfg`, `hosts.ini`, `site.yml`.
- Блокеры: нет

### 2026-07-16 10:24 - TASK_011 - done

- Что сделано: создан файл `Kubernetes/contexts.md` со статьей о переключении контекстов в Kubernetes; обновлен `Kubernetes/README.md`; `TASK_011.md` переведен в `done` и подготовлен к переносу в `Tasks/`.
- Где остановились: задача завершена, статья добавлена в раздел `Kubernetes`.
- Что дальше: при необходимости добавить отдельные материалы по RBAC, kubeconfig auth plugins, `kubectx`/`kubens` или безопасному shell prompt для Kubernetes.
- Блокеры: нет

### 2026-07-16 10:18 - TASK_011 - todo

- Что сделано: заведена задача `TASK_011.md` на подготовку статьи о переключении контекстов в Kubernetes; для завершенных задач выделен архивный каталог `Tasks/`, а ссылки в `TASK.md` обновлены под новую схему хранения.
- Где остановились: постановка задачи готова, сама статья в разделе `Kubernetes` еще не создана.
- Что дальше: подготовить материал `Kubernetes/contexts.md` с объяснением `kubeconfig`, `contexts`, `current-context`, переключения через `kubectl config use-context` и безопасной проверки текущего кластера.
- Блокеры: нет

### 2026-06-01 18:14 - TASK_010 - done

- Что сделано: создан каталог `GlusterFS` и файл `GlusterFS/basic-setup.md` с инструкцией по GlusterFS: основные сущности, схема кластера, требования, подготовка нод, установка пакетов, trusted pool, создание replicated/distributed/arbiter volume, client mount, fstab, проверка чтения/записи, диагностика и базовое обслуживание.
- Где остановились: задача завершена, `TASK_010.md` переведен в `done`, критерии готовности отмечены, `TASK.md` и `README.md` обновлены.
- Что дальше: при необходимости добавить отдельные материалы по production tuning, мониторингу GlusterFS, Kubernetes CSI или автоматизации через Ansible.
- Блокеры: нет

### 2026-05-03 20:27 - TASK_009 - done

- Что сделано: создан файл `Docker/architecture.md` с описанием устройства Docker, ролей Docker CLI, `dockerd`, containerd, containerd shim и runc, а также с разбором `docker run`, images/layers/snapshots, storage driver, network stack, volumes и Docker Desktop.
- Где остановились: задача завершена, материал добавлен в раздел `Docker`, критерии готовности отмечены в `TASK_009.md`.
- Что дальше: при необходимости вынести отдельные подробные материалы по OCI runtime spec, containerd/ctr, rootless Docker или сетям Docker.
- Блокеры: нет

### 2026-05-03 00:00 - TASK_009 - todo

- Что сделано: заведена задача на подготовку материала об устройстве Docker, его основных компонентах, роли Docker CLI, `dockerd`, containerd, runc и shim.
- Где остановились: создано описание задачи `TASK_009.md`, сам материал в разделе `Docker` еще не подготовлен.
- Что дальше: создать заметку `Docker/architecture.md` с объяснением архитектуры Docker, схемой взаимодействия компонентов и диагностическими командами.
- Блокеры: нет

### 2026-04-29 20:13 - TASK_008 - done

- Что сделано: создан файл `Docker/volumes.md` с инструкцией по Docker volumes, отличием от bind mount, командами управления, подключением через `-v` и `--mount`, примерами с PostgreSQL и Nginx, копированием данных, бэкапом через `tar` и очисткой volumes.
- Где остановились: задача завершена, отдельный материал по Docker volumes подготовлен.
- Что дальше: при необходимости добавить отдельную заметку по volumes в Docker Compose и production-подходам к бэкапу баз данных.
- Блокеры: нет

### 2026-04-29 20:11 - TASK_008 - todo

- Что сделано: заведена задача на подготовку отдельной инструкции по Docker volumes в разделе `Docker`.
- Где остановились: задача описана, материал `Docker/volumes.md` еще не создан.
- Что дальше: подготовить инструкцию с объяснением volumes, отличием от bind mount, командами управления, примерами подключения и сохранения данных.
- Блокеры: нет

### 2026-04-29 19:46 - TASK_007 - done

- Что сделано: создан каталог `Docker` и файл `Docker/basic-commands.md` с основными командами для работы с Docker: проверка установки, контейнеры, образы, логи, `exec`, порты, переменные окружения, монтирование каталогов, сети, тома, копирование файлов и очистка ресурсов.
- Где остановились: задача завершена, базовый раздел Docker подготовлен.
- Что дальше: при необходимости добавить отдельные материалы по Dockerfile, Docker Compose, публикации образов и настройке Docker daemon.
- Блокеры: нет

### 2026-04-29 19:43 - TASK_007 - todo

- Что сделано: заведена задача на создание отдельного каталога `Docker` и подготовку материала по основным командам Docker.
- Где остановились: задача описана, каталог `Docker` и сам материал еще не созданы.
- Что дальше: создать каталог `Docker`, добавить markdown-файл с командами для контейнеров, образов, логов, сетей, томов и очистки ресурсов.
- Блокеры: нет

### 2026-04-29 19:34 - TASK_006 - done

- Что сделано: создан файл `Linux/archives.md` с командами для работы с архивами: `tar`, `gzip`, `bzip2`, `xz`, `zip`, `unzip`, примерами создания, просмотра, распаковки, исключения файлов, проверки целостности и распаковки с учетом прав.
- Где остановились: задача завершена, базовый материал по архивам в Linux подготовлен.
- Что дальше: при необходимости добавить отдельную заметку по `7z`, `rar`, `zstd`, шифрованию архивов и стратегиям резервного копирования.
- Блокеры: нет

### 2026-04-24 12:20 - TASK_005 - done

- Что сделано: создан файл `Postgres/users_and_roles.md` с разбором ролей и пользователей, примерами создания, изменения, удаления, выдачи и отзыва прав, а также с типовыми ролями `read_only` и `admin`.
- Где остановились: задача завершена, базовый материал по управлению пользователями в PostgreSQL подготовлен.
- Что дальше: при необходимости добавить отдельную заметку по `pg_hba.conf`, аудиту привилегий и безопасности паролей.
- Блокеры: нет

### 2026-04-24 12:10 - TASK_005 - todo

- Что сделано: заведена задача на подготовку материала по работе с пользователями и ролями в PostgreSQL, создан файл `TASK_005.md`.
- Где остановились: задача описана, сам материал в разделе `Postgres` еще не создан.
- Что дальше: подготовить отдельную заметку с командами `CREATE ROLE`, `ALTER ROLE`, `GRANT`, `REVOKE` и примерами проверки прав.
- Блокеры: нет

### 2026-04-24 12:00 - TASK_004 - done

- Что сделано: созданы отсутствующие тематические каталоги верхнего уровня, добавлены `README.md` и `TASK_TEMPLATE.md`, исправлены внутренние ссылки в task-файлах.
- Где остановились: cleanup завершен, репозиторий приведен к консистентному состоянию и готов к дальнейшему наполнению.
- Что дальше: при необходимости добавить индексные страницы по разделам и отдельный шаблон для технических заметок.
- Блокеры: нет

### 2026-04-23 00:10 - TASK_003 - done

- Что сделано: создан файл `Linux/find.md` с инструкцией по команде `find` и примерами поиска по имени, типу, размеру, времени изменения и через `-exec`.
- Где остановились: задача завершена, материал для раздела `Linux` подготовлен.
- Что дальше: при необходимости добавить отдельную заметку по `xargs`, `locate` и безопасному удалению найденных файлов.
- Блокеры: нет

### 2026-04-23 00:05 - TASK_003 - todo

- Что сделано: задача `TASK_003` переопределена с темы сортировки строк на инструкцию по команде `find`.
- Где остановились: постановка задачи обновлена, сам материал еще не был создан.
- Что дальше: подготовить отдельный файл в разделе `Linux` с примерами `-name`, `-type`, `-mtime`, `-size` и `-exec`.
- Блокеры: нет

### 2026-04-22 00:50 - TASK_003 - done

- Что сделано: создан файл `Linux/sort.md` с объяснением сортировки строк в Linux и примерами `sort`, `uniq`, `head`, `tail`, `column`.
- Где остановились: задача завершена, материал для раздела `Linux` подготовлен.
- Что дальше: при необходимости добавить отдельные заметки по `uniq`, `comm`, `join` и сортировке логов.
- Блокеры: нет

### 2026-04-22 00:40 - TASK_003 - todo

- Что сделано: заведена задача на подготовку объяснения сортировки строк в Linux.
- Где остановились: задача описана, сам материал еще не подготовлен.
- Что дальше: создать материал для раздела `Linux` с объяснением команды `sort` и примерами.
- Блокеры: нет

### 2026-04-22 00:30 - TASK_002 - done

- Что сделано: создан файл `Postgres/active_queries.sql` с запросами для просмотра активных запросов, длительных запросов, ожиданий и блокирующих PID через `pg_stat_activity`.
- Где остановились: задача завершена, базовый SQL-набор для диагностики активности PostgreSQL готов.
- Что дальше: при необходимости добавить отдельные запросы по блокировкам, долгим транзакциям и статистике из `pg_stat_statements`.
- Блокеры: нет

### 2026-04-22 00:20 - TASK_002 - todo

- Что сделано: зафиксирована задача на создание SQL-файла в каталоге `Postgres` для просмотра активных запросов в PostgreSQL.
- Где остановились: постановка задачи оформлена, сам SQL-файл еще не создан.
- Что дальше: дождаться команды на создание файла в `Postgres` и добавить в него SQL-запросы для просмотра активных запросов.
- Блокеры: нет

### 2026-04-22 00:10 - TASK_001 - done

- Что сделано: созданы каталоги `Python`, `Airflow`, `Kubernetes`, `Postgres`, `Kafka`, `Linux`, `Ansible`, `Terraform`, `Git`, `Vault`, `Elasticsearch`; заполнен `TASK_001.md`; обновлен `TASK.md`.
- Где остановились: задача завершена, базовая структура репозитория готова.
- Что дальше: определить шаблон стартовых страниц или вложенной структуры для каждого раздела wiki.
- Блокеры: нет

### YYYY-MM-DD HH:MM - TASK_001 - todo

- Что сделано: создан шаблон задачи.
- Где остановились: содержимое задачи еще не заполнено.
- Что дальше: описать задачу в `TASK_001.md` и перевести статус в `in_progress`, когда работа начнется.
- Блокеры: нет
